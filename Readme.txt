//TypeScript - super conjunto de JavaScript - Microsoft

Lenguaje actual:
	ECMAScript 2018
	ES8

Convierte código de TypeScript a JavaScript (la versión que indiquemos) para que los navegadores lo entiendan.

Los errores se muestran mientras se escribe el código.

//Instalacion:
npm i typescript -g

//Ver la version:
tsc --version

//convertir codigo TS a JS
tsc salida.ts
//Creara un archivo salida.js


